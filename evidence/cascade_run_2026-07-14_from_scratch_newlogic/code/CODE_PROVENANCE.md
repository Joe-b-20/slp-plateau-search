# Code provenance -- cascade run (2026-07-14, from scratch, new logic; 92@4, 91@5)

- Same as the sub-89 run: `mixcolumns_core.py` / `engines.py` exact;
  `worker.py` / `ladder_parallel.py` are the current versions and are
  behaviourally identical for this run (per-worker seed/knob support, added
  later, is inert when unused). Reproduce with the config below + config.json.
